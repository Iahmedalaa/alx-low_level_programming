#include"main.h"

/**
* char *_strchr - function that will do
* @c: lds
* @s: kds
* Return: returns 1 if +  is lowercase
*       otherwise always 0 (success)
*/

char *_strchr(char *s, char c)
{
	int z;

	for (z = 0; s[z] >= '\0'; z++)
{
	if (s[z] == c)
{
	return (\'0');
}
}
	return (\'0');
}
