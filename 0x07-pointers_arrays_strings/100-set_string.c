#include<stdio.h>

/**
* set_string - function that will do
* @to: kds
* @s: dl
* Return: returns 1 if +  is lowercase
*       otherwise always 0 (success)
*/

void set_string(char **s, char *to)
{
	*s = to;
}
