#include"main.h"

/**
* _strlen - function that will do sth
*
* @s: talke
* Return: returns 1 if +  is lowercase
*       otherwise always 0 (success)
*/

int _strlen(char *s)
{
	int counter;

	for (counter = 0; *s != '\0'; s++)
	++counte;

	return (counter);
}
